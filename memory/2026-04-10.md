# 2026-04-10 — Head Renderer Refactor + Fusion Quaternion Discovery

## Head Model Positioning Refactor (DONE)
- Refactored `HeadModelRenderer` from parent-child approach to scene-sibling approach
- headGroup is now added directly to the scene, not as child of wingsuit model
- Position: wingsuit world pos + neck offset rotated by wingsuit world quat
- Rotation: head inertial quat directly (no parent-chain inversion needed)
- Body frame scene: passes `bodyToInertial` quat so head shows relative rotation (`bodyQuat⁻¹ × headInertialQuat`)
- `update()` signature: `(gpsTime, wingsuitWorldPos, wingsuitWorldQuat, modelScale, bodyToInertial?)`
- tsc passes clean after refactor

## Fusion Quaternion — Key Finding (NOT YET IMPLEMENTED)
- Current head-renderer converts Euler angles (roll/pitch/yaw) from fused CSV through NWU→NED conversion, then `bodyToInertialQuat()` — lossy, ambiguous sign flips
- The fused CSV already contains quaternion components: `qw, qx, qy, qz` (NWU body-to-earth, scalar-first)
- Fusion viewer (`sensor_fusion_handoff/fusion_viewer/src/viewer.ts`) uses quaternion DIRECTLY:
  ```
  setOrientation(q): deviceGroup.quaternion.set(-q.y, q.z, -q.x, q.w)
  ```
  This is NWU→Three.js component remap: `(-qy, qz, -qx, qw)`
- **Next step**: Use quaternion directly from CSV with same remap, skip Euler angles entirely
- Only difference from fusion viewer: 180° heading flip because sensor is on back of helmet (pre-rotation on quat)
- Hartman confirmed head looks better after sibling refactor but still not matching fusion viewer output

## Head Quaternion Direct Implementation (DONE)
- Replaced Euler-based `getHeadInertialQuat()` with `getHeadThreeQuat()` using direct CSV quaternion
- NWU→Three.js remap: `(-qy, qz, -qx, qw)` — proven from fusion viewer
- Added `MOUNT_PITCH_DEG = 10` — body-frame correction for sensor mount angle on helmet, applied as Hamilton product with Y-axis rotation before remap
- Removed redundant 180° yaw pre-rotation (HEAD_MODEL_ROTATION Euler `(π/5, 0, π)` already handles back-of-helmet flip)
- Removed `bodyToInertialQuat` import — no longer needed

## Blended Roll Method (DONE)
- Default roll changed from `'full'` to `'blended'` in `DEFAULT_CANOPY_CONFIG` and HTML dropdown
- Phase-based: full transversal during deployment (ΔF_b=0, accurate), coordinated turn after full inflation
- 2-second smoothstep (cubic hermite `t²(3-2t)`) transition at deployment boundary
- `deployEndIndex`/`deployEndTime` added to `CanopyEstimatorConfig`
- Deploy detection reordered before canopy estimation in `gps-main.ts`
- Wingsuit pitch lerp: 0.5s smoothstep from flying pose to canopy hang at line stretch

## Sensor Fusion Timestamp Alignment Fix (IN PROGRESS)
- **Problem**: Fused CSV `timestamp` column is raw FlySight sensor clock (seconds since device boot), NOT normalized to 0. The 07-29-25 file starts at 152650s (~42h uptime). Working 03-27-26 file happened to start near 0 (fresh boot).
- **$TIME entries in SENSOR.CSV**: `$TIME,sensorTime,tow,week` — links sensor clock to GPS week/TOW. This is how `timestampSync.ts` computes `sensorToGpsOffsetMs`.
- **Fix part 1 — sensor fusion export** (`fusedDataExport.ts`): Added `gps_time` column (ISO 8601 absolute UTC) computed via `sensorToGpsOffsetMs` from `gpsState.syncResult`. Every row gets absolute time.
- **Fix part 2 — GPS viewer**: 
  - `head-sensor.ts`: Added `gpsTimeMs` field, parser reads `gps_time` column, normalizes `t` to start at 0, returns `gpsStartIndex`
  - `gps-pipeline.ts`: Added `startEpochMs` to `PipelineResult` (first GNSS fix absolute time)
  - `gps-main.ts`: Auto-alignment: `offset = (sensorGpsTimeMs - pipelineEpochMs)/1000 - sensorT`. Falls back to position-based if no `gps_time` column.
- **Action needed**: Re-export 07-29-25 fused CSV from sensor fusion viewer to get new `gps_time` column
- GPS viewer commit pending after testing
