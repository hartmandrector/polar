# 2026-03-11 Session 2 — Deploy Gamepad, Unzip, Line Twist

## Completed
- **Weight shift direction fix** (`68a718a`) — inverted wsSign so left input → left turn
- **Deploy gamepad** (`0d56d00`) — `readDeployGamepad()`: no brakes, 25% risers, weight shift, twist, B=unzip
- **Unzip state machine** (`0d56d00`) — B button → 1.5s ramp, riserRange 25%→100%, brakeAccess 0%→100%
- **HUD overlay** (`0d56d00`) — deploy phase: brake stow, limited controls, flashing "PRESS B TO UNZIP", unzip progress bar
- **Line twist 3D rendering** (`72264b3`) — `pilotPivot.rotation.y` nested inside pitch, slider ±360°
- **Line twist HUD** (`fe95d9f`) — direction, rate, recovery indicator. Amber >10°, red >90°
- **Status docs updated** (`908f28b`) — STATUS.md + WINGSUIT-BASE-FLOW.md phases 1-3.5 marked complete
- **Unzip drag morph wired** (`2cf2049` + `b1643cf`) — `unzipProgress` → `config.controls.unzip` + `FlightState.delta` for polar chart sync
- **Line twist torsional coupling** (`32c0115`) — canopy yaw rate drives twist via `tau_yaw = -coupling * r`, coupling=5 N·m·s/rad

## Key Findings
- Unzip aero morph was already built in segment factory (`makeUnzippablePilotSegment`) but wasn't wired to sim controls — just needed `config.controls.unzip = unzipProgress`
- Polar chart reads from FlightState (not sim controls), so also needed `delta: unzipProgress` in flight update
- Twist coupling at 5 N·m·s/rad produces ~7° at moderate turn rate, can build to 2000°+ with aggressive maneuvers
- Real-world recovery techniques work emergently: asymmetric riser/brake input speeds recovery
- Recovery rate ~17°/s observed during testing

## Architecture Notes
- `pilotTwistEOM` now takes `canopyYawRate` and `yawCoupling` — no feedback to canopy aero (safe)
- Deploy gamepad mode selected by `canopyDeploy.hasFullControls` flag
- Unzip progress drives both physics (`config.controls.unzip`) and display (`FlightState.delta`)
